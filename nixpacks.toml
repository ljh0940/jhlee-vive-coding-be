[phases.setup]
nixPkgs = ['openjdk21']

[phases.build]
cmds = ['./gradlew clean build -x test']

[start]
cmd = 'java -Dserver.port=$PORT -Dspring.profiles.active=railway -jar build/libs/jhlee-vive-coding-be-0.0.1-SNAPSHOT.jar'
